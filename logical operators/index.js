// = assignment
// == comparison
// === strict equality (value and datatype)
// != not equal
// !== strict not equal
// && and
// || or
// ! not



const temp = 20;
if (temp>0 && temp < 30){
    console.log("The weather is good");
}
else if(temp <= 0 || temp >= 30){
    console.log("The weather is bad");
}
else if (temp != 30 || temp == 0){
    console.log("weather is weathering");
}